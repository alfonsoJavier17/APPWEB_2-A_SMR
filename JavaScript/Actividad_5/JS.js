document.addEventListener("DOMContentLoaded", function(){

    function esprimo (num)(

    for(let i=2; i<num;i++)(
        if(num%i == 0)(
            return false;
        )
    )
           return true
        )

    const num_1 =parseInt(promp("Introduce un numero entero:"))
    const num_2 =parseInt(promp("Introduce otro numero entero:"))
    let max,min;

    if (num_1 > num_2)
        max=num_1;
        min=num_2;
    else
        max=num_2;
        min=num_1;
    
    for let i=min; i <= max, i++
});